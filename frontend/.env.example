# Base URL for the API server.
# Leave empty to use the Vite dev server proxy (recommended for local dev).
# Set to the full origin when building for production without a reverse proxy,
# e.g. VITE_API_URL=http://api:8000
VITE_API_URL=
